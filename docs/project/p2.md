# Project 1: Creating a Frontend

This project assignment is worth 600 points.

This homework is to be done as a team.

P2 is due on Gradescope Thursday, March 26, 2026 11:59pm.

### Submission

Submit [P2](https://www.gradescope.com/courses/1208559/assignments/) as a PDF file to Gradescope.

If you prepare the response in some other software (like Tex), please
export as PDF before submitting. Include your name and Andrew ID at the top of the document.

### Learning Goals


- Design and implement the front-end user interface of your startup's primary application.
- Use a Large Language Model (LLM) to generate software artifacts, including user stories, UI components, and application code.
- Use an LLM to generate human-verifiable documentation (wireframes and development specifications) that describe the behavior of software artifacts produced by the LLM.
- Create machine-verifiable test cases that demonstrate that an implemented user interface correctly satisfies the intended user stories and development specification.

### 1. Project Context

In modern software engineering workflows, LLMs can generate code and interfaces very quickly. However, this creates an important challenge: **how do we verify that the generated artifacts actually implement what the designer intended?**

In this project assignment, you will:

1. Use an LLM to generate the **user stories** describing your product's application.
2. Use the LLM to generate the **UI and first version of the frontend code**.
3. Use the LLM to create machine-executable test cases (unit tests and acceptance tests) 
that show that the UI and code satisfies the requirements described in the user stories.  
4. Use the LLM to create documentation for the UI and code you have created that will
convince you and your users that you have built the product defined by your user stories.

### 2. User Stories (100 points)

In this section, you will create user stories that correspond to the primary use cases of the product. You can write the user stories by yourselves or use an LLM to write them for you.

The user stories should be customized by your understanding of the primary use cases of your startup's main product as well as the lessons you learned from your user discovery process. 

  1. The total user stories your team should write is 2 times the number of team members. 
  1. Create this number of user stories. Make sure each user story uses the proper template:
    - As a <user\>, I want <action\> so that <benefit\>. Then add your machine and human acceptance criteria afterwards.
    - Add a one paragraph explanation of why each user story is important to the overall user experience of the application. 
    - Make sure each user story can be designed, implemented, tested, and verified in under 2 weeks.
    - If your team has 4 members, create 8 user stories. 5 members? Create 10 user stories. 6 members? Create 12 user stories.
  2. Evaluate the user stories according to the [INVEST](https://xp123.com/invest-in-good-stories-and-smart-tasks/) framework we learned in class. 
  3. Identify ths 3 user stories that you think are most critical to the business success of the application and explain why.
  4. Identify the 3 user stories that you think will be easiest to implement and explain why.
  5. Identify the 3 user stories that you think will be easiest to test via machine-executable tests and explain why.
  6. Identify the 3 user stories that you think will be easiest for human to decide that it was implemented satisfactorily and explain why.
  7. Prioritize the user stories in order that you should implement them. Justify your prioritization order.  
     1. Identify the "core" user story that creates the base functionality of your application. This must be your first user story to implement.
  8. Create GitHub Issues for each user story (along with its acceptance criteria) in your team's GitHub project. Be sure to assign each user story a priority level, specify the sprint number in which it will be completed, and assign it to primary and secondary owners on your team.  
  9. If you used an LLM for any part of this section, please turn in a copy of the chat log or a URL to the chatlog that is accessible to all of the 17-356 instructors.


### 3. User Interfaces (100 points)

Create high-fidelity user interfaces for each of your user stories using Figma, Canva, Pencil, Bolt, or Lovable. 

<!-- Pick one user story out of the total and create 3 high-fidelity user interfaces for it that are maximally different from each other. Present these user interfaces to a classmate from another team and have that person give feedback about the components of each UI that they like the best (i.e. I like the colors in UI 1, the shapes of the buttons in UI 2, and the infromation flow in UI 3). Do not provide feedback about what you do not like. From this feedback, produce a fourth user interface for this user story that includes the well-liked components from each of the other three UIs.  -->

   1. Include all states of the interface: e.g., empty state, loading, error, success, etc.  
   2. Include enough detail about the intended graphical design in your Figma project to help a developer to implement the appropriate CSS and layout code. For example, if your design needs to relayout in a specific way when the window is stretched or zoomed, please annotate the design with the behavior you want to see.
   3. Write up the rationale for major design choices (e.g., navigation flow, component reuse, accessibility). 
   3. For each element in the UI, identify which user story(ies) required it. If it cannot be connected to a user story, the element should be removed from the interface.
   4. Be sure that the design of each user interface is compatible with all of the others, meaning it should look like one coherently designed application. This requires effective communication between all of the designers on your team as well as any LLMs you may be consulting for help.
   4. Turn in a URL to each user interface prototype (this could be in Figma or Canva or other tool).
   5. Check in high-resolution screenshots of your user interface prototypes into your GitHub repository and turn in URLs to each pull request (PR) merging them into your main branch. Enter the user interfaces for each user story in its own PR. There should be a total of 10 PRs (even if the user interfaces for each user story overlap).
   6. If you used an LLM for any part of this section, please turn in a copy of the chat log or a URL to the chatlog that is accessible to all of the 17-356 instructors.
   
 
### 4. Frontend Implementation (100 points)

Implement the user stories in code using a popular frontend framework of your choice (e.g., React, Svelte).  

   1. For each user story, create a new branch in Git. Write appropriate front end code for this user story. Update the status of the user story in your Kanban board so your teammates know what you are working on.
   2. Your implementation must: 
       1. Include responsive design for at least two screen sizes.  
       1. Ensure accessibility compliance with WCAG 2.2 and alt-text for images where appropriate.  
       1. Ensure any backend functionality or data is mocked such that the frontend functionality can be demonstrated.
   5. The core user story should be checked into your GitHub repository first. 
   6. Check in the code for each user story in its feature branch. 
   7. When all machine- and human-verifiable tests have succeeded, wrap up the commits into a pull request (PR) on GitHub and submit it. 
   8. Do a code review on the PR and iterate until your teammates are satisfied with the quality of the code.
   9. Approve the PR and merge it into main. Remmeber to keep the status of the user story up to date on your Kanban board.
   10. Turn in a URL to each user story's PR.
   11. Screen record yourself showing off the frontend implementation to a customer in a web browser or mobile device (include appropriate voiceover narration so we can follow what you're trying to show). It should display correctly at your two different screen sizes. Use the browser or mobile device's debugger to accurately set the screen size during testing. Resize the browser/mobile device window with your mouse/finger to test for proper layout logic. 
   12. Upload the screen recording to YouTube.
   13. If you used an LLM for any part of this section, please turn in a copy of the chat log or a URL to the chatlog that is accessible to all of the 17-356 instructors.


### 4. Frontend Specification (100 points)

Choose **3 user stories** and have an LLM document them into independent dev specs. Each dev spec should include the following sections with enough detail that you (or someone else) could answer the following questions: 

  1. identify the primary and secondary owners of the user story.
  2. Identify the date the code was merged into the main branch.
  3. Create an architecture diagram in Mermaid that illustrates where the components of the application execute (e.g., client, server, cloud, etc.). 
  4. Create an information flow diagram in Mermaid that shows how information flows between components. 
  5. Create a class diagram in Mermaid that shows the relevant classes in superclass/subclass relationships. This diagram must include *all* implenented classes and interfaces.
  6. Create a list of all of the classes in the implementation relevant to this user story. Each class must include a list of public and private fields and methods with explanations for the purpose for each. Group the public fields and methods together and then list the private fields and methods.
  7. Provide a complete list of technologies used in your system that you aren’t writing yourself.
     1. Indicate what the technology is being used for.
     2. Indicate why that technology was picked over others.
     3. Provide a URL to source location, author, and documentation for each technology.
        1. Don’t leave out technologies like language, common libraries, or necessary tools.
     4. Do mention the required version number for each technology.
  7. List each data type that you will be storing in a database. Explain the purpose of each field in the database. How much storage (in bytes)will it require?
  7. Create a list of the user-visible and internally-visible effects if your frontend applicaiton:
     1. crashed its process
     2. lost all its runtime state
     3. erased all stored data
     4. noticed that some data in the database appeared corrupt
     5. remote procedure call failed
     6. client overloaded
     7. client out of RAM
     8. database out of space
     9. lost its network connectivity
     10. lost access to its database
     11. bot signs up and spams users
  8. List all Personally Identifying Information (PII) stored in long-term storage in your system. 
     1. Justify why you need to keep it in storage.
     2. How exactly is it stored?
     3. Where did the data enter your system?
     4. Through what modules, components, classes, methods, and fields did it move before entering long-term storage?
     5. Through what modules, components, classes, methods, and fields will it move after leaving long-term storage?
     6. List the people on your team who have responsibility for securing each unit of long-term storage/database.
     7. Describe your procedures and data structures for auditing routine and non-routine access to the PII data.
     8. Is the PII of a minor under the age of 18 solicited or stored by the system?
        1. Why?
        2. Do you have a guardian’s permission to have that PII?
        3. What is your organization’s policy for ensuring that PII is not accessible by anyone convicted or suspected of child abuse?


### 5. Machine-Executable Test Cases (100 points)

TBD: Unit tests and acceptance tests


### 6. Human-Verifiable Test Cases (100 points)

TBD: Procedures for a human to validate whether the user story has been satisfied by this front end implementation.

